<!DOCTYPE html>
<html>
  <head>
    <title>CoinETFs | All you need to know about Bitcoin ETFs</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="description"
      content="Explore the latest Bitcoin ETF news. CoinETFs provides everything
      you should know about cryptocurrency Exchange-Traded Funds (ETFs).">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload', defer: true %>
  </head>

  <body>
    <% if current_page?("/") %>
      <%= render 'shared/home_navbar' %>
    <% else %>
      <%= render 'shared/navbar' %>
    <% end %>
    <%= render 'shared/flashes' %>

    <%= yield %>
  </body>
</html>
